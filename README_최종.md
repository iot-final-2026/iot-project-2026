# 🍫 초콜릿·사탕 자동 분류 및 생산 모니터링 시스템

## 1. 프로젝트 개요

### 1.1 프로젝트 소개

본 프로젝트는 컨베이어벨트를 기반으로 초콜릿과 사탕을 자동으로 감지하고 분류하며, 제품의 생산량을 실시간으로 관리할 수 있는 **IoT 기반 자동 생산 시스템**이다.

적외선 센서를 이용하여 컨베이어벨트 위의 제품을 감지하고, 카메라를 통해 제품 이미지를 촬영한 후 OpenCV 기반의 이미지 분류 기능을 이용하여 초콜릿과 사탕을 자동으로 구분한다.

분류된 제품은 컨베이어벨트를 통해 각각의 포장 위치로 이동하며, 설정된 수량만큼 제품이 적재되면 박스 적재 완료를 판단하고 서보모터를 이용하여 박스를 배출한다.

또한 작업자용 LCD 화면에서는 현재 생산량, 목표 생산량, 완료 세트 수 및 장비 상태를 확인할 수 있으며, 관리자용 웹 페이지에서는 생산 현황, 제품 분류 결과, 생산 통계, 알람 및 장비 상태를 실시간으로 확인할 수 있도록 구성한다.

---

### 1.2 개발 배경 및 필요성

기존의 생산 및 포장 공정에서는 제품의 분류와 수량 확인을 작업자가 직접 수행하는 경우가 많아 반복적인 작업에 많은 인력이 필요하다.

특히 제품의 종류가 다양해질수록 작업자의 판단에 의존한 수작업 분류 과정에서 다음과 같은 문제가 발생할 수 있다.

- 제품 분류 과정에서의 작업자 실수
- 생산량 및 포장 수량 확인에 필요한 시간 증가
- 생산 현황을 실시간으로 확인하기 어려움
- 장비 이상 발생 시 즉각적인 대응 어려움
- 생산 데이터를 체계적으로 기록하고 관리하기 어려움

이러한 문제를 해결하기 위해 센서, 카메라, 컨베이어벨트, 마이크로컨트롤러 및 서버를 연동하여 제품의 감지부터 분류, 생산량 집계, 박스 배출 및 모니터링까지 자동화하는 시스템을 개발한다.

---

### 1.3 프로젝트 목표

본 프로젝트의 주요 목표는 다음과 같다.

- 적외선 센서를 이용한 제품 자동 감지
- 카메라를 이용한 제품 이미지 촬영
- OpenCV를 이용한 초콜릿 및 사탕 자동 분류
- 컨베이어벨트를 이용한 제품 자동 이송
- 제품별 생산량 실시간 집계
- 설정 수량 도달 시 박스 적재 완료 감지
- 서보모터를 이용한 박스 자동 배출
- LCD, LED, 부저를 이용한 작업자 알림
- 관리자 웹 페이지를 통한 실시간 생산 모니터링
- 제품 이미지 및 분류 결과 저장
- 생산 데이터 및 오류 이력 데이터베이스 관리
- 생산 과정에서 발생하는 장비 오류 및 이상 상황 기록

---

### 1.4 팀원 및 역할

| 이름 | 역할 | 담당 업무 |
|------|------|-----------|
| 하서영 | 팀장 / 시스템 통합 및 연동 | 시스템 전체 구조 설계, 통합 테스트 및 오류 수정 |
| 김채현 | 프론트엔드 / 웹 | 관리자 웹 페이지 개발, 생산 현황 및 통계 화면 구현, 장비 상태 및 알림 화면 구현 |
| 김민주 | 백엔드, 데이터베이스 | ASP.NET Core Backend 개발, REST API 구현, MySQL 데이터베이스 설계 및 연동, 생산·분류 데이터 관리 |
| 박소영 | qt / 작업자 UI | 	Qt 기반 작업자 UI 개발, 생산량 및 목표량 표시, 장비 상태 및 알림 화면 구현 |
| 한동현 | IoT / 하드웨어 | Arduino 및 Raspberry Pi 제어, 센서·카메라·컨베이어·서보모터 연동, LCD·LED·부저 제어 |

---

## 2. 요구사항 및 설계

### 2.1 요구사항 정의

본 시스템은 작업자와 관리자로 사용자를 구분하며, 사용자 권한에 따라 사용할 수 있는 기능을 구분한다.

#### 작업자

- 생산 작업 시작 및 종료
- 컨베이어벨트 제어
- 생산량 확인
- 목표 생산량 확인
- 완료 세트 수 확인
- 장비 연결 상태 확인
- 생산 완료 및 이상 상황 알림 확인

#### 관리자

- 실시간 생산 현황 확인
- 제품 분류 결과 확인
- 제품 촬영 이미지 확인
- 생산 통계 확인
- 장비 상태 확인
- 오류 및 알람 내역 확인
- 생산 목표 설정

---

### 2.2 기능 요구사항

| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| FR-01 | 로그인 | 작업자 및 관리자 로그인 | 높음 |
| FR-02 | 로그아웃 | 로그인 사용자 로그아웃 | 중간 |
| FR-03 | 권한 구분 | 작업자와 관리자 권한 구분 | 높음 |
| FR-04 | 물체 감지 | 적외선 센서를 이용한 제품 감지 | 높음 |
| FR-05 | 이미지 촬영 | 제품 감지 시 카메라 촬영 | 높음 |
| FR-06 | 제품 분류 | OpenCV를 이용한 초콜릿 및 사탕 분류 | 높음 |
| FR-07 | 컨베이어 제어 | 컨베이어 시작, 정지 및 속도 제어 | 높음 |
| FR-08 | 자동 분류 | 분류 결과에 따른 제품 이송 | 높음 |
| FR-09 | 박스 적재 감지 | 설정 수량 도달 여부 확인 | 높음 |
| FR-10 | 생산량 집계 | 제품별 생산량 실시간 집계 | 높음 |
| FR-11 | 박스 배출 | 서보모터를 이용한 박스 배출 | 높음 |
| FR-12 | 작업자 UI | 생산량 및 장비 상태 표시 | 높음 |
| FR-13 | 이상 알림 | 센서 및 장비 이상 알림 | 높음 |
| FR-14 | 실시간 모니터링 | 관리자 웹에서 생산 현황 확인 | 높음 |
| FR-15 | 분류 결과 | 이미지 및 분류 신뢰도 확인 | 높음 |
| FR-16 | 생산 통계 | 목표 및 실제 생산량 통계 확인 | 중간 |
| FR-17 | 데이터 저장 | 생산 및 분류 데이터를 DB에 저장 | 중간 |

---

### 2.3 비기능 요구사항

- 제품 감지 후 분류 결과를 빠르게 표시할 수 있어야 한다.
- 생산 현황은 실시간으로 갱신되어야 한다.
- 센서, 카메라, 컨베이어 등의 장비가 안정적으로 동작해야 한다.
- 장비 이상 발생 시 작업자에게 즉시 알림을 제공해야 한다.
- 사용자 비밀번호는 암호화하여 저장해야 한다.
- 작업자와 관리자의 접근 권한을 구분해야 한다.
- 각 기능은 유지보수가 가능하도록 모듈화하여 개발한다.
- 아두이노, 라즈베리파이, 카메라 및 서버 간 통신이 정상적으로 이루어져야 한다.

---

### 2.4 UI 설계

#### 작업자 화면

작업자 화면은 생산 현장에서 현재 생산 상태를 빠르게 확인하고 장비를 제어할 수 있도록 구성한다.

주요 표시 정보는 다음과 같다.

- 현재 날짜 및 시간
- 작업자 정보
- 서버 및 장비 연결 상태
- 초콜릿 생산량
- 사탕 생산량
- 목표 생산량
- 완료 세트 수
- 생산 진행률
- 컨베이어 속도
- 생산 시작 및 정지
- 알림

![작업자 UI](./docs/UI_작업자_초안.png)

---

#### 관리자 화면

관리자 화면은 생산 현황 및 장비 상태를 통합적으로 확인할 수 있는 웹 대시보드 형태로 구성한다.

주요 메뉴는 다음과 같다.

- 대시보드
- 제품 이미지
- 생산 현황
- 생산 통계
- 알람 및 이상 내역
- 장비 상태
- 설정
- 작업자별 정보

![관리자 UI](./docs/UI_관리자_초안.png)

---

### 2.5 데이터베이스 설계

본 프로젝트의 데이터베이스는 생산 목표, 작업자별 생산 작업, 개별 제품 감지 및 분류 결과, 시스템 구성요소 상태, 알림 이력을 관리하도록 설계하였다.

MySQL을 사용하며, 생산 작업을 중심으로 각 데이터가 연결되도록 구성하였다.

주요 테이블은 다음과 같다.

```text
테이블	설명
users	관리자 및 작업자 계정과 권한 관리
product_types	초콜릿, 사탕 등 제품 유형 및 세트 구성 기준 관리
system_components	센서, 카메라, 컨베이어 등 시스템 구성요소 상태 관리
production_targets	하루 전체 생산 목표 및 작업 인원 관리
production_sessions	작업자별 생산 작업 및 생산량 관리
product_detections	개별 제품의 감지 및 분류 결과 관리
alerts	생산 및 시스템에서 발생한 정보, 경고, 오류 이력 관리
```

데이터베이스는 각 테이블이 독립적인 기본 키를 가지는 비식별 관계로 구성하였다.

#### 2.5.1 ERD

프로젝트의 데이터베이스 구조와 테이블 간 관계는 다음 ERD를 기준으로 설계하였다.

실제 MySQL 데이터베이스에 생성된 테이블과 PK·FK 관계는 DBeaver를 이용하여 확인할 수 있다.

관련 파일은 다음과 같다.

```text
smart_sorting_system.sql
smart_sorting_system.erd
smart_sorting_system_erd.png
smart_sorting_system_dbeaver_erd.png
```

#### 2.5.2 테이블 구성

users — 사용자
관리자와 작업자의 로그인 정보 및 권한을 관리한다.

```text
컬럼	설명
user_id	사용자 번호
login_id	로그인 아이디
password_hash	BCrypt 비밀번호 해시
name	사용자명
role	사용자 역할
created_at	계정 생성 일시
```

주요 규칙은 다음과 같다.

```text
login_id
→ UNIQUE

role
→ ADMIN
→ WORKER
```

비밀번호는 평문으로 저장하지 않고 BCrypt 방식으로 해시하여 저장한다.

##### product_types — 제품 유형

초콜릿과 사탕 등의 제품 유형 및 세트 구성 기준을 관리한다.

```text
컬럼	설명
product_type_id	제품 유형 번호
product_type_code	API 및 MQTT 연동용 제품 코드
product_name	제품명
unit_per_set	한 세트를 구성하는 낱개 수
created_at	생성 일시
```

초기 제품 유형은 다음과 같이 구성한다.

```text
제품 코드	제품명	세트당 개수
CHOCOLATE	초콜릿	10
CANDY	사탕	1
```

unit_per_set은 0보다 큰 값을 사용한다.

제품 종류에 따른 세트 수량을 데이터베이스에서 관리하기 때문에 향후 다른 제품 유형이 추가되더라도 제품별 세트 구성 기준을 유연하게 변경할 수 있다.

##### system_components — 시스템 구성요소

시스템에서 사용하는 하드웨어 및 소프트웨어 구성요소의 현재 상태를 관리한다.

```text
컬럼	설명
component_id	구성요소 번호
component_code	API 및 MQTT 연동용 고정 코드
component_name	구성요소 이름
component_type	구성요소 유형
current_status	현재 상태
status_updated_at	상태 갱신 일시
```

구성요소 유형은 다음과 같다.

```text
SENSOR
ACTUATOR
CONTROLLER
DISPLAY
SOFTWARE
SERVER
DATABASE
```

현재 상태는 다음과 같이 관리한다.

```text
NORMAL  : 정상
WARNING : 경고
ERROR   : 오류
OFFLINE : 연결 또는 통신 불가
```

주요 관리 대상은 다음과 같다.

```text
Raspberry Pi
Arduino
적외선 센서
Camera
Conveyor
Servo Motor
Buzzer
작업자 WPF
관리자 Web
MQTT Broker
ASP.NET Core Server
MySQL Database
```

status_updated_at은 구성요소가 등록될 때 저장하며 장비 상태가 변경될 때 current_status와 함께 갱신한다.

##### production_targets — 생산 목표

하루 전체 생산 목표와 작업 인원 및 다음 날 적용할 예약 목표를 관리한다.

```text
컬럼	설명
target_id	생산 목표 번호
target_chocolate_set_count	현재 적용 중인 초콜릿 목표 세트 수
target_candy_set_count	현재 적용 중인 사탕 목표 세트 수
next_target_chocolate_set_count	다음 날 적용할 초콜릿 목표 세트 수
next_target_candy_set_count	다음 날 적용할 사탕 목표 세트 수
daily_worker_count	현재 적용 중인 하루 작업 인원
next_daily_worker_count	다음 날 적용할 작업 인원
updated_at	마지막 수정 일시
```

하루 전체 목표는 production_targets에서 관리하며, 작업자가 생산을 시작하면 daily_worker_count를 기준으로 각 생산 세션에 목표량을 분배한다.

```text
production_targets
        ↓
하루 전체 생산 목표
        ↓
작업 인원 기준 목표 분배
        ↓
production_sessions
```

생산이 이미 시작된 이후 관리자가 목표 또는 작업 인원을 변경하는 경우에는 next_* 컬럼에 값을 저장한다.

다음 날 첫 생산 세션이 시작되면 예약된 next_* 값을 새로운 목표로 적용하고, 적용이 완료된 예약값은 NULL로 초기화한다.

##### production_sessions — 생산 작업

작업자 한 명이 수행하는 하나의 생산 작업 단위를 관리한다.

```text
컬럼	설명
session_id	생산 작업 번호
user_id	작업자 번호
target_chocolate_set_count	해당 세션의 초콜릿 목표 세트 수
target_candy_set_count	해당 세션의 사탕 목표 세트 수
chocolate_count	생산된 초콜릿 낱개 수
candy_count	생산된 사탕 낱개 수
status	생산 작업 상태
started_at	작업 시작 일시
ended_at	작업 종료 일시
updated_at	마지막 수정 일시
```

생산 작업 상태는 다음과 같이 관리한다.

```text
RUNNING   : 생산 진행 중
PAUSED    : 일시 정지
COMPLETED : 생산 완료
CANCELLED : 생산 취소
```

주요 규칙은 다음과 같다.

```text
- 목표량과 생산량은 음수가 될 수 없다.

- 생산 세션이 시작될 때 하루 전체 목표를 작업 인원 기준으로 분배하여 세션 목표를 저장한다.

- RUNNING, PAUSED 상태에서는 작업이 종료되지 않은 상태로 관리한다.

- COMPLETED, CANCELLED 상태에서는 작업 종료 시각을 기록한다.

- 이전 날짜에 종료되지 않은 생산 세션은 새로운 생산 작업 시작 시 CANCELLED 처리한다.

- 생산량은 실제 제품의 낱개 수를 기준으로 관리한다.

chocolate_count
→ 초콜릿 낱개 생산량

candy_count
→ 사탕 낱개 생산량

- 세트 수는 제품 유형의 unit_per_set을 기준으로 계산한다.

- 세트 수 = 생산 낱개 수 / unit_per_set
```

##### product_detections — 제품 감지 및 분류 결과

제품이 감지되고 카메라 촬영 및 이미지 분류가 수행될 때마다 개별 감지 결과를 저장한다.

```text
컬럼	설명
product_detection_id	제품 감지 번호
session_id	생산 작업 번호
product_type_id	제품 유형 번호
confidence	분류 신뢰도
image_path	촬영 이미지 경로
classification_status	분류 상태
detected_at	제품 감지 일시
```

분류 상태는 다음과 같다.

```text
SUCCESS : 분류 성공
FAILED  : 분류 실패
```

제품 감지 데이터는 다음과 같은 흐름으로 저장된다.

```text
제품 감지
   ↓
카메라 촬영
   ↓
이미지 분류
   ↓
product_detections 저장
```

주요 규칙은 다음과 같다.

```text
- 제품 감지 결과는 생산 세션과 연결한다.
- confidence는 0 이상 1 이하의 값을 사용한다.
- SUCCESS인 경우 정상적으로 분류된 제품 유형을 저장한다.
- FAILED인 경우 제품 유형을 특정할 수 없으므로 product_type_id가 NULL일 수 있다.
- 이미지가 저장되지 않은 경우 image_path는 NULL일 수 있다.
- 제품 분류 실패와 장비 오류는 서로 다른 개념으로 관리한다.
- product_detections는 제품별 개별 생산 이력을 담당하고, production_sessions는 세션 단위 생산량 집계를 담당한다.
```

```text
product_detections
        ↓
개별 제품 생산 이력

production_sessions
        ↓
작업자별 생산량 집계
```

##### alerts — 알림

생산 과정 및 시스템에서 발생하는 정보, 경고, 오류의 이력을 관리한다.

```text
컬럼	설명
alert_id	알림 번호
session_id	관련 생산 작업 번호
component_id	관련 시스템 구성요소 번호
product_detection_id	관련 제품 감지 번호
checked_by_user_id	알림을 확인한 사용자 번호
alert_type	알림 유형
priority	중요도
error_code	오류 코드
recovery_status	복구 상태
check_status	확인 상태
alert_message	알림 상세 내용
created_at	발생 일시
recovered_at	복구 일시
checked_at	확인 일시
```

알림 유형은 다음과 같다.

```text
INFO
WARNING
ERROR
```

중요도는 다음과 같이 관리한다.

```text
LOW
MEDIUM
HIGH
```
```text
복구 상태:

NOT_RECOVERED : 미복구
RECOVERED     : 복구 완료

확인 상태:

UNCHECKED : 미확인
CHECKED   : 확인 완료

INFO 유형의 알림은 복구 또는 확인 대상이 아닌 경우 관련 상태 및 시간값을 NULL로 사용할 수 있다.

오류 코드는 장비 및 시스템 오류 종류를 식별하기 위해 사용한다.

예:

CAMERA_ERROR
NO_DETECTION
SERIAL_DISCONNECTED
```

주요 규칙은 다음과 같다.

```text
- RECOVERED 상태가 되면 recovered_at을 기록한다.
- 미복구 상태에서는 recovered_at을 NULL로 유지한다.
- 사용자가 알림을 확인하면 checked_by_user_id와 checked_at을 기록한다.
- 자동 생성된 장비 알림은 해당 구성요소가 정상 상태로 복구되면 자동으로 RECOVERED 처리할 수 있다.
- 수동으로 생성된 알림은 자동 복구 대상에서 제외한다.
- 동일 구성요소와 동일 오류 코드의 미복구 알림은 중복 생성하지 않는다.
```

#### 2.5.3 테이블 관계

각 테이블의 관계는 다음과 같다.

```text
부모 테이블	자식 테이블	관계	자식 FK
users	production_sessions	1:N	NOT NULL
production_sessions	product_detections	1:N	NOT NULL
product_types	product_detections	1:N	NULL 가능
production_sessions	alerts	1:N	NULL 가능
system_components	alerts	1:N	NULL 가능
product_detections	alerts	1:N	NULL 가능
users	alerts	1:N	NULL 가능

production_targets는 하루 전체 목표를 독립적으로 관리하기 때문에 다른 테이블과 직접적인 외래키 관계를 가지지 않는다.

전체 관계를 간단하게 표현하면 다음과 같다.

                    production_targets
                           │
                           │ 목표 분배
                           ▼
users ──────── 1:N ─── production_sessions
                           │
                           │ 1:N
                           ▼
                    product_detections
                           │
                           │ N:1
                           ▼
                     product_types
```

```text
system_components ── 1:N ──┐
                            │
users ─────────── 1:N ──────┤
                            ▼
production_sessions ─ 1:N → alerts
                            ▲
product_detections ─ 1:N ──┘
```


#### 2.5.4 생산 목표 및 세션 관리

생산 목표는 하루 전체 목표와 작업자별 목표를 구분하여 관리한다.

```text
[production_targets]

하루 전체 생산 목표
        │
        ↓
daily_worker_count
작업 인원 확인
        │
        ↓
작업자별 목표 분배
        │
        ↓
[production_sessions]
세션별 목표 저장
        │
        ↓
제품 생산
```

예를 들어 하루 초콜릿 목표가 100세트이고 작업자가 2명이라면 작업자별 생산 세션에 목표를 분배하여 저장한다.

```text
> 하루 목표

초콜릿 100세트
사탕 50세트

        ↓

작업자 2명

        ↓

작업자 A
초콜릿 50세트
사탕 25세트

작업자 B
초콜릿 50세트
사탕 25세트
```

각 생산 세션은 작업 시작 당시의 목표를 별도로 저장하기 때문에 이후 전체 목표가 변경되더라도 이미 시작된 생산 작업의 목표값을 유지할 수 있다.


#### 2.5.5 제품 감지 및 생산량 처리

제품이 컨베이어를 따라 이동하면 적외선 센서가 제품을 감지한다.

```text
제품 투입
   ↓
적외선 센서 감지
   ↓
카메라 촬영
   ↓
이미지 처리
   ↓
초콜릿 / 사탕 분류
   ↓
product_detections 저장
   ↓
production_sessions 생산량 증가
```

정상적으로 분류된 제품은 product_detections에 저장하고, 제품 유형에 따라 해당 생산 세션의 생산량을 증가시킨다.

```text
CHOCOLATE
→ chocolate_count + 1

CANDY
→ candy_count + 1

세트 수는 product_types.unit_per_set을 기준으로 계산한다.

초콜릿
10개 = 1세트

사탕
1개 = 1세트
```

따라서 제품별 생산량을 직접 세트 수로 저장하지 않고 실제 생산된 낱개 수를 기준으로 관리한다.

#### 2.5.6 박스 적재 및 완료 처리

제품의 세트 구성 기준은 product_types.unit_per_set에서 관리한다.

초콜릿은 다음과 같이 처리한다.

```text
초콜릿 1개
   ↓
생산량 +1
   ↓
현재 박스 수량 확인
   ↓
10개 도달?
   ↓
YES
   ↓
1세트 완료
   ↓
박스 적재 완료
   ↓
서보모터 동작
   ↓
박스 배출
   ↓
LED / 부저 / LCD 알림
   ↓
다음 박스 생산 시작

> 초콜릿의 경우

10개 = 1세트

사탕의 경우:

1개 = 1세트
```

세트 구성 기준은 제품 유형 테이블에서 관리하기 때문에 생산 로직에서 제품별 수량을 직접 하드코딩하지 않도록 구성한다.

박스 배출이 완료되면 현재 박스의 적재 수량을 초기화하고 다음 생산 작업을 진행한다.

박스가 가득 찬 경우 작업자에게 박스 교체가 필요하다는 알림을 제공한다.

#### 2.5.7 장비 상태 및 오류 처리

시스템에서 사용하는 센서, 카메라, 컨베이어, 서보모터 등의 장비는 system_components에서 관리한다.

```text
system_components
        │
        ├── SENSOR
        ├── ACTUATOR
        ├── CONTROLLER
        ├── DISPLAY
        ├── SOFTWARE
        ├── SERVER
        └── DATABASE
```

각 구성요소는 다음 상태 중 하나를 가진다.

```text
NORMAL
WARNING
ERROR
OFFLINE
```

장비에 오류가 발생하면 해당 구성요소의 상태를 변경하고 alerts에 오류 이력을 저장한다.

```text
장비 오류 발생
      ↓
system_components.current_status 변경
      ↓
alerts 오류 이력 저장
      ↓
작업자 UI 알림
      ↓
LCD / LED / 부저 알림
      ↓
관리자 Web 표시
```

장비가 정상적으로 복구되면 구성요소 상태를 NORMAL로 변경하고 관련 자동 생성 알림을 RECOVERED로 처리할 수 있다.

#### 2.5.8 알림 처리

alerts는 생산 작업, 제품 감지, 시스템 구성요소와 독립적으로 연결될 수 있도록 설계하였다.

따라서 다음과 같은 상황을 모두 저장할 수 있다.

```text
생산 작업 관련 알림
        ↓
production_sessions

제품 분류 관련 알림
        ↓
product_detections

장비 관련 알림
        ↓
system_components

알림 확인 사용자
        ↓
users
```

모든 알림이 반드시 생산 작업 또는 제품과 관련되어야 하는 것은 아니므로 관련 외래키에는 NULL을 허용한다.

예를 들어 서버 연결 오류와 같이 특정 제품이나 생산 작업과 관계없는 오류도 저장할 수 있다.

```text
SERVER_CONNECTION_ERROR

session_id = NULL
product_detection_id = NULL
component_id = Backend Server
```

반대로 특정 제품의 분류 실패와 관련된 알림은 제품 감지 데이터와 연결할 수 있다.

```text
제품 분류 실패
      ↓
product_detections
      ↓
alerts
```

알림 확인이 완료되면 확인한 사용자와 확인 시간을 기록한다.

```text
alerts
   ↓
checked_by_user_id
   ↓
users
```

#### 2.5.9 데이터 처리 흐름

전체 데이터 처리 과정은 다음과 같다.

```text
사용자 로그인
    ↓
production_targets 조회
    ↓
생산 작업 시작
    ↓
production_sessions 생성
    ↓
적외선 센서 제품 감지
    ↓
카메라 이미지 촬영
    ↓
FastAPI / OpenCV 이미지 처리
    ↓
제품 분류
    ↓
product_detections 저장
    ↓
생산량 증가
    ↓
production_sessions 갱신
    ↓
목표 수량 및 세트 수 확인
    ↓
박스 적재
    ↓
세트 완료 확인
    ↓
서보모터 박스 배출
    ↓
LCD / LED / 부저 알림
    ↓
관리자 Web에 생산 현황 표시
```

#### 2.5.10 IoT 데이터 처리 흐름

IoT 장비에서 발생하는 데이터는 Backend를 중심으로 처리한다.

```text
IoT 장비
   ↓
적외선 센서
   ↓
제품 감지
   ↓
카메라 촬영
   ↓
이미지 데이터 전달
   ↓
FastAPI Server
   ↓
OpenCV 이미지 처리
   ↓
제품 분류
   ↓
분류 결과 반환
   ↓
ASP.NET Core Backend
   ↓
product_detections 저장
   ↓
production_sessions 생산량 갱신
   ↓
MySQL Database
   ↓
 ┌───────────────┐
 │               │
 ▼               ▼
작업자 WPF     관리자 Web

장비 상태 및 오류가 발생하는 경우 다음과 같이 처리한다.

IoT 장비
   ↓
장비 상태 변화
   ↓
Backend
   ↓
system_components 상태 갱신
   ↓
alerts 이력 저장
   ↓
작업자 / 관리자 화면 표시
```

## 3. 개발환경 구축


### 3.1 개발환경 개요

본 프로젝트는 IoT 장비, Backend Server, 이미지 처리 서버, 데이터베이스, 작업자 UI 및 관리자 Web을 각각 분리하여 개발한다.

각 구성요소는 API를 통해 데이터를 주고받도록 구성하여 시스템 간 결합도를 낮추고 유지보수성을 높인다.

### 3.2 개발환경

```text
구분	사용 기술
작업자 UI	WPF
Backend	ASP.NET Core
이미지 처리	FastAPI
이미지 처리 라이브러리	OpenCV
데이터베이스	MySQL
IoT 제어	Arduino / Raspberry Pi
센서	적외선 센서
영상 장치	Camera
출력 장치	LCD / LED / Buzzer
구동 장치	Servo Motor
협업	Git / GitHub
개발 도구	Visual Studio / VS Code
API 테스트	Postman
DB 관리	MySQL Workbench
문서 관리	Markdown
```

### 3.3 GitHub 저장소 구축

프로젝트의 소스 코드와 문서를 GitHub를 이용하여 관리한다.

GitHub Repository에는 다음과 같은 구조로 프로젝트를 관리한다.

```text
IoT-Final-Project
│
├── README.md
│
├── docs
│   ├── 01-requirements.md
│   ├── 02-ui-design.md
│   ├── 03-database-design.md
│   └── ...
│
├── backend
│   └── ASP.NET Core Project
│
├── fastapi
│   └── Image Classification Server
│
├── worker-ui
│   └── WPF Project
│
├── iot
│   └── Arduino / Raspberry Pi
│
└── tests
    └── Test Projects
```

### 3.4 브랜치 관리

개발 과정에서 기능별로 브랜치를 분리하여 개발한다.

```text
main
 │
 ├── develop
 │    │
 │    ├── feature/login
 │    ├── feature/production
 │    ├── feature/classification
 │    ├── feature/iot
 │    ├── feature/worker-ui
 │    └── feature/admin-web
 │
 └── release

- main
최종적으로 배포 및 시연 가능한 안정적인 코드만 관리한다.

- develop
각 기능 개발이 완료된 후 통합하는 개발 브랜치이다.

- feature
각 팀원이 담당 기능을 개발하기 위한 브랜치이다.

예시:

feature/login
feature/sensor
feature/camera
feature/classification
feature/production
feature/worker-ui
feature/admin-dashboard
```

## 3.5 개발환경 설치 및 설정

프로젝트 개발을 위해 각 팀원의 개발 PC에 필요한 개발환경을 구축한다.

```text
Backend

.NET SDK 설치
ASP.NET Core 프로젝트 생성
필요한 NuGet 패키지 설치
데이터베이스 연결 설정
API 서버 실행 확인
FastAPI
Python 설치
가상환경 생성
FastAPI 설치
OpenCV 설치
이미지 처리 모듈 구현
API 실행 확인
WPF
Visual Studio 설치
WPF 프로젝트 생성
필요한 라이브러리 설치
Backend API 연결 설정
작업자 화면 구현
IoT
Arduino IDE 설치
Raspberry Pi 환경 설정
센서 연결
카메라 연결
LCD 연결
LED 및 부저 연결
서보모터 연결
컨베이어 모터 연결
Database
MySQL 설치
데이터베이스 생성
테이블 생성
초기 데이터 입력
Backend DB 연결 확인
```

# 4. 실제 개발

### 4.1 기능별 담당자

프로젝트는 기능별로 담당자를 지정하여 개발한다.

```text
기능	담당자	개발 내용
프로젝트 관리	팀장	전체 일정 및 개발 진행 관리
Backend	팀장	ASP.NET Core API 및 서버 개발
Database	담당자	DB 설계 및 데이터 관리
IoT	담당자	센서 및 장비 제어
이미지 분류	담당자	FastAPI / OpenCV 개발
작업자 UI	담당자	WPF 화면 개발
관리자 Web	담당자	관리자 대시보드 개발
테스트	전체	기능 및 통합 테스트
문서	전체	요구사항 및 개발 문서 작성

※ 실제 팀원 이름과 역할은 프로젝트 진행 상황에 맞게 수정한다.
```

### 4.2 로그인 기능 개발

로그인 기능은 작업자와 관리자 계정을 구분하여 시스템 접근 권한을 관리하기 위해 개발한다.

사용자가 로그인 화면에서 아이디와 비밀번호를 입력하면 Backend API를 통해 사용자 정보를 확인한다.

로그인이 성공하면 사용자의 권한에 따라 접근 가능한 기능을 구분한다.

```text
로그인
  ↓
ID / Password 입력
  ↓
Backend API 요청
  ↓
USERS 조회
  ↓
로그인 정보 확인
  ↓
권한 확인
  ├── 작업자 → 작업자 UI
  └── 관리자 → 관리자 Web

개발 항목
로그인 API 구현
사용자 정보 조회
비밀번호 검증
관리자 / 작업자 권한 구분
로그인 실패 처리
로그아웃 기능
```

### 4.3 적외선 센서 감지 기능 개발

컨베이어벨트 위로 제품이 지나갈 때 적외선 센서를 이용하여 제품의 존재 여부를 감지한다.

센서에서 제품이 감지되면 카메라 촬영을 요청하고 이후 이미지 분류 과정을 진행한다.

```text
센서 대기
   ↓
제품 감지
   ↓
감지 신호 발생
   ↓
카메라 촬영 요청

테스트 항목
제품이 없을 때 오작동 여부
제품 투입 시 정상 감지 여부
연속 제품 감지 여부
센서 연결 오류 여부
```

### 4.4 카메라 촬영 기능 개발

적외선 센서가 제품을 감지하면 카메라를 통해 제품 이미지를 촬영한다.

촬영된 이미지는 이미지 분류 서버로 전달한다.

촬영 이미지에는 제품 분류 시간 등의 정보를 연결하여 데이터 추적이 가능하도록 구성한다.

### 4.5 이미지 분류 기능 개발

촬영된 제품 이미지는 FastAPI 서버를 통해 전달되고 OpenCV를 이용하여 이미지 처리를 수행한다.

이미지 처리 결과를 바탕으로 제품을 초콜릿 또는 사탕으로 분류한다.

```text
Camera
  ↓
Image
  ↓
FastAPI
  ↓
OpenCV
  ↓
Image Processing
  ↓
Classification
  ↓
Chocolate / Candy
  ↓
Confidence
```

분류 결과에는 다음 정보가 포함된다.

```text
제품 종류
분류 신뢰도
이미지 경로
분류 시간
분류 성공 여부
```

### 4.6 자동 분류 기능 개발

AI 분류 결과에 따라 제품을 해당 위치로 이동시킨다.

예를 들어 분류 결과가 초콜릿이면 초콜릿 적재 위치로 이동시키고, 사탕이면 사탕 적재 위치로 이동시킨다.

```text
제품 감지
   ↓
이미지 촬영
   ↓
AI 분류
   ↓
 ┌───────────────┐
 │               │
초콜릿          사탕
 │               │
 ↓               ↓
초콜릿 위치     사탕 위치
```

### 4.7 생산량 집계 기능 개발

제품이 정상적으로 분류되면 제품 종류에 따라 생산량을 증가시킨다.

초콜릿은 개별 수량과 세트 수량을 함께 관리한다.

```text
초콜릿 생산량 = 초콜릿 개별 제품 수
초콜릿 세트 수 = 초콜릿 생산량 / 10
```

사탕은 1개를 1세트로 관리한다.

```text
사탕 생산량 = 사탕 개별 제품 수
사탕 세트 수 = 사탕 생산량
```

생산량 데이터는 생산 세션별로 관리하며 데이터베이스에 저장한다.

### 4.8 박스 완료 및 배출 기능 개발

초콜릿의 경우 10개가 적재되면 하나의 박스 또는 세트가 완료된 것으로 처리한다.

박스가 완료되면 서보모터를 동작시켜 박스를 배출하고 작업자에게 알림을 제공한다.

```text
제품 적재
   ↓
수량 확인
   ↓
10개 도달?
   ↓
YES
   ↓
세트 완료
   ↓
서보모터 동작
   ↓
박스 배출
   ↓
LED / 부저 / LCD 알림
```

### 4.9 작업자 WPF UI 개발

작업자 UI는 생산 현장에서 작업자가 필요한 정보를 빠르게 확인할 수 있도록 구성한다.

```text
- 주요 화면
- 로그인 화면
- 작업자 메인 화면
- 생산 현황
- 라인 제어
- 알림 화면
- 작업자 메인 화면

┌──────────────────────────────────────────┐
│  IoT 자동 분류 시스템       2026-00-00   │
│  작업자: 홍길동              연결: 정상   │
├──────────────────────────────────────────┤
│                                          │
│  [ 초콜릿 ]                 [ 사탕 ]      │
│                                          │
│  현재 108개                 현재 80개     │
│  목표 200개                 목표 100개    │
│  진행률 54%                 진행률 80%    │
│                                          │
├──────────────────────────────────────────┤
│                                          │
│  라인 속도       50%                     │
│                                          │
│       [ - ]      [ + ]                   │
│                                          │
│       [ 시작 ]    [ 정지 ]               │
│                                          │
└──────────────────────────────────────────┘
```

### 4.10 관리자 Web 개발

관리자 Web에서는 생산 현황을 한눈에 확인할 수 있도록 대시보드 형태로 개발한다.

```text
주요 기능
실시간 생산량
목표 생산량
달성률
생산량 그래프
제품 분류 이미지
AI 신뢰도
알람 내역
장비 상태
작업자 정보
관리자 대시보드 구성
┌──────────────────────────────────────────┐
│              관리자 Dashboard             │
├──────────────┬──────────────┬────────────┤
│ 초콜릿 생산량 │ 사탕 생산량  │ 전체 생산량 │
│    108개     │    80개      │   188개    │
├──────────────┴──────────────┴────────────┤
│                                          │
│             생산량 추이 그래프            │
│                                          │
├─────────────────────┬────────────────────┤
│ 최근 분류 이미지     │ 장비 상태          │
│                     │ 센서   정상        │
│ 초콜릿 95%           │ 카메라 정상        │
│ 사탕 91%             │ 컨베이어 정상      │
│                     │ 모터   정상        │
├─────────────────────┴────────────────────┤
│             최근 알람 내역                │
└──────────────────────────────────────────┘
```

### 4.11 GitHub Commit 관리

GitHub를 이용하여 기능 개발 과정과 변경 이력을 관리한다.

Commit 메시지는 작업 내용을 쉽게 확인할 수 있도록 일정한 규칙을 사용한다.

```text
Commit 규칙
feat: 새로운 기능 추가
fix: 오류 수정
refactor: 코드 구조 개선
docs: 문서 수정
test: 테스트 코드 작성 및 수정
style: 코드 스타일 수정
chore: 기타 설정 및 관리 작업

Commit 예시
feat: 로그인 API 구현
feat: 적외선 센서 데이터 수신 기능 추가
feat: 카메라 촬영 기능 구현
feat: 초콜릿 사탕 분류 API 구현
feat: 생산량 집계 기능 추가
feat: 작업자 WPF 생산 현황 화면 구현
feat: 관리자 대시보드 구현

fix: 센서 중복 감지 오류 수정
fix: 생산량 증가 오류 수정
fix: 카메라 연결 오류 수정

test: 로그인 API 단위 테스트 추가
test: 제품 분류 테스트 추가

docs: 요구사항 정의서 수정
docs: 데이터베이스 설계서 작성
```

### 4.12 Pull Request 관리

기능 개발이 완료되면 담당자가 개발 브랜치의 코드를 Pull Request로 등록한다.

Pull Request 등록 후 다른 팀원이 코드를 확인하고 문제가 없는 경우 develop 브랜치에 Merge한다.

```text
feature/branch
      ↓
개발 완료
      ↓
Commit
      ↓
Pull Request
      ↓
Code Review
      ↓
수정 사항 반영
      ↓
Merge
      ↓
develop
```

### 4.13 기능별 단위 테스트

각 기능은 개발 완료 후 단위 테스트를 수행한다.

```text
- 로그인 테스트

테스트 항목	입력	예상 결과
정상 로그인	올바른 ID/PW	로그인 성공
잘못된 비밀번호	잘못된 PW	로그인 실패
존재하지 않는 ID	없는 ID	로그인 실패
권한 확인	관리자 계정	관리자 화면 접근
권한 확인	작업자 계정	작업자 화면 접근

- 센서 테스트
테스트 항목	예상 결과
제품 미감지	촬영하지 않음
제품 감지	촬영 시작
연속 제품	각 제품 정상 감지
센서 오류	오류 알림 발생

- 이미지 분류 테스트
테스트 항목	예상 결과
초콜릿 이미지	초콜릿으로 분류
사탕 이미지	사탕으로 분류
잘못된 이미지	분류 실패 처리
분류 신뢰도 확인	신뢰도 정상 반환

- 생산량 테스트
테스트 항목	예상 결과
초콜릿 1개 생산	초콜릿 수량 +1
초콜릿 10개 생산	1세트 완료
사탕 1개 생산	사탕 수량 +1
목표량 도달	목표 달성 상태 표시

- 알림 테스트
테스트 항목	예상 결과
박스 가득 참	LCD/LED/부저 알림
센서 오류	오류 알림
카메라 오류	오류 알림
컨베이어 오류	오류 알림
```

# 5. 테스트 화면

### 5.1 테스트 개요

각 기능의 개발이 완료된 후 실제 장비와 소프트웨어를 연동하여 통합 테스트를 진행한다.

테스트는 다음과 같은 순서로 진행한다.

```text
로그인
  ↓
생산 시작
  ↓
제품 투입
  ↓
센서 감지
  ↓
카메라 촬영
  ↓
제품 분류
  ↓
컨베이어 이동
  ↓
생산량 증가
  ↓
DB 저장
  ↓
LCD 표시
  ↓
관리자 Web 표시
  ↓
박스 완료
  ↓
알림 및 박스 배출
```

### 5.2 로그인 테스트 화면

```text
- 테스트 내용

작업자 계정 로그인
관리자 계정 로그인
잘못된 계정 로그인
권한별 화면 접근 확인
테스트 결과
테스트	결과
작업자 로그인	PASS
관리자 로그인	PASS
잘못된 비밀번호	PASS
권한 구분	PASS
```

### 5.3 작업자 화면 테스트

작업자 화면에서 생산량과 목표량이 정상적으로 표시되는지 확인한다.

```text
확인 항목
날짜 및 시간 표시
작업자 이름 표시
초콜릿 생산량
사탕 생산량
목표 생산량
진행률
컨베이어 속도
시작 / 정지 버튼
연결 상태
테스트 결과
테스트 항목	결과
생산량 표시	PASS
목표량 표시	PASS
진행률 표시	PASS
라인 시작	PASS
라인 정지	PASS
알림 표시	PASS
```

### 5.4 센서 및 컨베이어 테스트

제품을 컨베이어벨트에 투입하여 적외선 센서가 정상적으로 제품을 감지하는지 확인한다.

센서가 제품을 감지하면 카메라 촬영 및 제품 분류 과정이 정상적으로 시작되는지 확인한다.

```text
테스트 결과
테스트 항목	결과
센서 감지	PASS
컨베이어 동작	PASS
제품 이동	PASS
연속 제품 처리	PASS
```

### 5.5 카메라 및 이미지 분류 테스트

카메라를 이용하여 제품 이미지를 촬영하고 FastAPI 서버로 전달한 후 OpenCV 기반 분류가 정상적으로 이루어지는지 확인한다.

```text
테스트 결과
제품	예상 결과	실제 결과	결과
초콜릿	초콜릿	초콜릿	PASS
사탕	사탕	사탕	PASS
초콜릿	초콜릿	초콜릿	PASS
사탕	사탕	사탕	PASS
```

### 5.6 생산량 테스트

제품을 반복적으로 투입하여 생산량이 정확하게 증가하는지 확인한다.

초콜릿은 10개가 생산되었을 때 1세트가 완료되는지 확인하고, 사탕은 1개 생산 시 1세트가 증가하는지 확인한다.

```text
테스트 결과
테스트	예상 결과	결과
초콜릿 1개	1개 증가	PASS
초콜릿 10개	1세트 증가	PASS
사탕 1개	1개 증가	PASS
목표량 달성	목표 달성 표시	PASS
```

### 5.7 관리자 대시보드 테스트

관리자 페이지에서 IoT 장비에서 발생한 생산 데이터가 정상적으로 표시되는지 확인한다.

```text
확인 항목
현재 생산량
목표 생산량
생산 달성률
제품 분류 결과
분류 신뢰도
생산 그래프
최근 알람
장비 상태
테스트 결과
항목	결과
생산량 조회	PASS
목표량 조회	PASS
그래프 표시	PASS
이미지 조회	PASS
분류 결과 조회	PASS
장비 상태 조회	PASS
알람 조회	PASS
```

### 5.8 데이터베이스 저장 테스트

제품이 감지 및 분류된 후 관련 데이터가 데이터베이스에 정상적으로 저장되는지 확인한다.

저장 데이터는 다음과 같다.

```text
생산 세션
제품 종류
분류 신뢰도
이미지 경로
감지 시간
생산량
오류 및 알림
테스트 결과
데이터	저장 여부
생산 세션	PASS
제품 정보	PASS
분류 결과	PASS
이미지 경로	PASS
생산량	PASS
알람 정보	PASS
```

# 6. 테스트 결과

### 6.1 통합 테스트 결과

전체 시스템을 실제 IoT 장비와 연동하여 통합 테스트를 진행한다.

```text
[로그인]
   ↓
[생산 시작]
   ↓
[제품 투입]
   ↓
[센서 감지]
   ↓
[카메라 촬영]
   ↓
[AI 분류]
   ↓
[제품 자동 분류]
   ↓
[생산량 집계]
   ↓
[DB 저장]
   ↓
[작업자 UI 표시]
   ↓
[관리자 Web 표시]
   ↓
[박스 완료]
   ↓
[알림]
   ↓
[박스 배출]
```

### 6.2 기능 테스트 결과

```text
기능	테스트 결과
로그인	PASS
권한 구분	PASS
센서 감지	PASS
카메라 촬영	PASS
제품 분류	PASS
자동 분류	PASS
생산량 집계	PASS
박스 완료 감지	PASS
박스 배출	PASS
LCD 표시	PASS
관리자 Web	PASS
DB 저장	PASS
알림 기능	PASS
```

### 6.3 요구사항 테스트 결과

```text
요구사항	테스트 결과
FR-01 로그인	PASS
FR-02 로그아웃	PASS
FR-03 권한 구분	PASS
FR-04 물체 감지	PASS
FR-05 이미지 촬영	PASS
FR-06 제품 분류	PASS
FR-07 컨베이어 제어	PASS
FR-08 자동 분류	PASS
FR-09 박스 적재 완료	PASS
FR-10 생산량 집계	PASS
FR-11 박스 배출 및 알림	PASS
FR-12 작업자 UI	PASS
FR-13 이상 감지	PASS
FR-14 관리자 모니터링	PASS
FR-15 분류 결과 확인	PASS
FR-16 생산 통계	PASS
FR-17 생산 데이터 저장	PASS
```

# 7. 트러블 슈팅

### 7.1 센서 중복 감지 문제

```text
문제
제품 하나가 센서를 통과할 때 센서 신호가 여러 번 발생하여 제품 수량이 실제보다 많이 증가하는 문제가 발생하였다.

원인
센서가 제품을 감지하는 동안 짧은 시간 동안 여러 번 신호가 발생하는 현상이 발생하였다.

해결
센서가 한 번 감지된 이후 일정 시간 동안 추가 감지를 무시하는 방식으로 중복 감지를 방지하였다.
```

```text
센서 감지
   ↓
제품 처리
   ↓
일정 시간 Lock
   ↓
센서 재감지 허용

결과
제품 하나당 하나의 생산 데이터가 생성되도록 수정하였다.
```

### 7.2 카메라 촬영 타이밍 문제

```text
문제
센서가 제품을 감지한 직후 카메라를 촬영하면 제품이 카메라 위치에 도달하기 전에 촬영되어 제품 이미지가 제대로 나오지 않는 문제가 발생하였다.

원인
센서 위치와 카메라 위치 사이에 물리적인 거리가 존재하였다.

해결
센서 감지 후 일정 시간 지연을 적용하여 제품이 카메라 촬영 위치에 도달한 후 촬영하도록 수정하였다.
```

```text
센서 감지
   ↓
Delay
   ↓
카메라 촬영
   ↓
이미지 처리

결과
제품이 이미지 중앙에 위치한 상태에서 촬영할 수 있도록 개선하였다.
```

### 7.3 이미지 분류 오류

```text
문제
제품의 방향이나 조명 상태에 따라 초콜릿과 사탕의 분류 결과가 달라지는 문제가 발생하였다.

원인
촬영 환경의 조명과 제품의 위치가 일정하지 않았기 때문이다.

해결
촬영 환경의 조명을 일정하게 유지하고, 이미지 크기 및 색상 처리 등의 전처리 과정을 적용하였다.

결과
이미지 분류 결과의 안정성을 개선하였다.
```

### 7.4 생산량 동기화 문제

```text
문제
IoT 장비에서 표시되는 생산량과 관리자 Web에서 표시되는 생산량이 일치하지 않는 문제가 발생하였다.

원인
생산량 데이터를 각각의 프로그램에서 별도로 계산하고 있었기 때문이다.

해결
생산량의 기준 데이터를 데이터베이스로 통일하고 Backend API를 통해 작업자 UI와 관리자 Web에서 동일한 데이터를 조회하도록 수정하였다.

IoT
 ↓
Backend
 ↓
Database
 ↓
 ├── 작업자 UI
 └── 관리자 Web

결과
각 화면에서 동일한 생산량을 표시하도록 개선하였다.
```

### 7.5 서버 연결 오류

```text
문제
WPF 또는 IoT 장비에서 Backend Server에 연결하지 못하는 문제가 발생하였다.

원인
서버 주소 및 포트 설정이 개발 환경과 실제 장비 환경에서 다르게 설정되어 있었다.

해결
환경별 서버 주소를 설정 파일로 분리하고 API 연결 상태를 확인할 수 있는 기능을 추가하였다.

결과
서버 연결 상태를 쉽게 확인할 수 있도록 개선하였다.
```

### 7.6 박스 완료 처리 문제

```text
문제
초콜릿 10개가 적재된 이후에도 생산량이 계속 증가하여 다음 박스의 수량과 혼합되는 문제가 발생하였다.

원인
세트 완료 이후 현재 박스의 적재 수량을 초기화하지 않았다.

해결
10개가 적재되면 세트 완료 처리를 수행하고 현재 박스 수량을 초기화하도록 수정하였다.

초콜릿 10개
   ↓
세트 완료
   ↓
박스 배출
   ↓
현재 박스 수량 = 0
   ↓
다음 박스 생산 시작

결과
박스 단위 생산량을 정확하게 관리할 수 있도록 수정하였다.
```

# 8. 프로젝트 실행 방법

### 8.1 사전 준비

다음 프로그램 및 장비가 필요하다.

```text
Visual Studio
.NET SDK
Python
MySQL
MySQL Workbench
Arduino IDE
Raspberry Pi
Camera
적외선 센서
LCD
LED
Buzzer
Servo Motor
Conveyor Belt
8.2 데이터베이스 실행
MySQL을 실행한 후 프로젝트의 DB 스크립트를 이용하여 데이터베이스와 테이블을 생성한다.

Database
  ↓
USERS
PRODUCTION_SESSIONS
PRODUCTS
PRODUCT_TYPES
ALERTS
SYSTEM_COMPONENTS
```

### 8.3 Backend 실행

```text
ASP.NET Core 프로젝트를 실행한다.

Backend Project
      ↓
ASP.NET Core
      ↓
API Server 실행
```

실행 후 WPF와 FastAPI에서 Backend API에 정상적으로 접근할 수 있는지 확인한다.


### 8.4 FastAPI 실행

Python 가상환경을 활성화하고 필요한 패키지를 설치한다.

```text
Python
  ↓
Virtual Environment
  ↓
FastAPI
  ↓
OpenCV
  ↓
Image Classification Server
```

### 8.5 WPF 실행

Visual Studio에서 WPF 프로젝트를 실행한다.

실행 후 작업자 계정으로 로그인하여 작업자 화면이 정상적으로 표시되는지 확인한다.

### 8.6 IoT 장비 실행

Arduino 또는 Raspberry Pi에 작성한 프로그램을 업로드하고 각 장비의 연결 상태를 확인한다.

```text
Arduino / Raspberry Pi
        ↓
센서 연결 확인
        ↓
카메라 연결 확인
        ↓
LCD 연결 확인
        ↓
LED / 부저 확인
        ↓
서보모터 확인
        ↓
컨베이어 확인
```

### 8.7 전체 시스템 실행 순서

```text
1. MySQL 실행
        ↓
2. ASP.NET Core Backend 실행
        ↓
3. FastAPI 실행
        ↓
4. IoT 장비 실행
        ↓
5. WPF 작업자 프로그램 실행
        ↓
6. 관리자 Web 실행
        ↓
7. 작업자 로그인
        ↓
8. 생산 시작
        ↓
9. 제품 투입
        ↓
10. 자동 분류 및 생산량 집계
```

# 9. 스크린샷 및 시연영상

### 9.1 작업자 UI

작업자가 생산량과 목표 생산량을 확인하고 컨베이어벨트를 제어할 수 있는 화면이다.

![alt text](images/image.png)

### 9.2 관리자 Dashboard

관리자가 전체 생산 현황과 장비 상태를 확인할 수 있는 화면이다.

![alt text](images/image-1.png)

### 9.3 제품 분류 화면

카메라로 촬영한 제품과 AI 분류 결과 및 신뢰도를 확인할 수 있다.

![alt text](images/image-2.png)

### 9.4 알림 및 이상 상태 관리 화면

시스템 및 장비에서 발생한 알림과 이상 내역을 확인하고, 발생 시간과 처리 상태 및 상세 정보를 조회할 수 있는 화면이다.

![alt text](images/image-4.png)

### 9.5 IoT 장비

컨베이어벨트, 적외선 센서, 카메라, LCD, LED, 부저 및 서보모터가 구성된 실제 장비 사진이다.

![alt text](images/image-3.png)

### 9.6 시연영상

프로젝트 최종 시연영상 링크를 아래에 추가한다.

[시연영상 링크]